from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from database_setup import Base, Expires

engine = create_engine('sqlite:///expires.db')
Base.metadata.bind = engine
DBSession = sessionmaker(bind = engine)

#template
session = DBSession()
listing = Expires(ml='S348371',
                  address = '6 W Church ST',
                  city = 'Adams',
                  zip = '13605',
                  own_first = 'James',
                  own_last = 'Tryon',
                  list_date = '02/22/2016',
                  dom = 191,
                  phone = '5855895488')

#session.add(listing)
#session.commit()
print session.query(Expires).all()

